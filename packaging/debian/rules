#!/usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = easyrestore

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Packaging builds may not have GUI/display deps; skip here.
	@echo "Skipping dh_auto_test; run pytest in the source tree."
